# ✅ Your Gemini API Key (rotate the old one ASAP if leaked)
GEMINI_API_KEY=AIzaSyDWOhRoQjo_tp_MTrGUW5L4KIjuqE1n4pQ

# ✅ Model (can be changed if needed)
GEMINI_MODEL=gemini-2.5-flash

# ✅ Optional — size limits for file uploads
MAX_FILE_MB=2
MAX_FILES=20

# ✅ Optional — only needed if you want to support local dev
# In production, you can even remove this line completely.
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500,http://localhost:5173,http://localhost:3000,https://code-review-assistant-3ihw.onrender.com

# ✅ (Optional) Enable this temporarily to bypass Gemini calls for debugging
# Set to 1 to return a mock response. Remove or set to 0 for real reviews.
BYPASS_LLM=0


